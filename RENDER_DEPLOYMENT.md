# 🚀 Развертывание на Render.com

## Шаг 1: Подготовьте данные

Прежде чем начать, соберите две переменные:

### 1️⃣ GOLDEN_KEY (ключ FunPay)
**Что это:** Ваш личный ключ авторизации FunPay
**Где взять:**
1. Откройте https://funpay.com в браузере
2. Войдите в аккаунт
3. Откройте DevTools (F12 на Windows, Cmd+Option+I на Mac)
4. Перейдите в вкладку Application (или Storage)
5. Найдите Cookies → funpay.com
6. Найдите cookie с именем `golden_key`
7. Скопируйте значение (должно быть 32 символа)

**Примечание:** Выглядит так: `abc123def456abc123def456abc123d`

---

### 2️⃣ TG_TOKEN (токен Telegram бота)
**Что это:** Токен вашего личного Telegram бота
**Где взять:**
1. Откройте Telegram
2. Найдите @BotFather
3. Напишите `/newbot`
4. Следуйте инструкциям
5. Скопируйте полученный токен

**Примечание:** Выглядит так: `123456789:ABCDefGHIJKlmnoPQRstUVwxYZ`

---

## Шаг 2: Развертывание на Render

### Переход на Render

1. Откройте https://render.com
2. Зарегистрируйтесь или войдите
3. Нажмите кнопку **"+ New"** в левом меню
4. Выберите **"Web Service"**

### Подключение GitHub

1. Выберите **"Deploy existing repository from GitHub"**
2. Авторизуйте доступ Render к вашему GitHub
3. Найдите и выберите репозиторий `FunPayCardinal`
4. Выберите ветку `main`

### Настройка сервиса

В форме настройки установите:

| Поле | Значение |
|------|----------|
| **Name** | `funpay-cardinal-bot` |
| **Environment** | `Python 3` |
| **Build Command** | `pip install -r requirements.txt` |
| **Start Command** | `bash -c 'python setup_render.py && python restart_bot.py'` |
| **Plan** | `Free` |

### Установка переменных окружения ⚠️ ВАЖНО

1. Нажмите на кнопку **"Advanced"** (или найдите раздел Environment)
2. Добавьте две переменные:

#### Переменная 1:
```
Key:   GOLDEN_KEY
Value: [ваш ключ FunPay из шага 1]
```

#### Переменная 2:
```
Key:   TG_TOKEN
Value: [ваш токен Telegram бота из шага 2]
```

### Запуск развертывания

1. Нажмите кнопку **"Deploy"** 
2. Дождитесь завершения (может занять 1-2 минуты)
3. Откройте вкладку **"Logs"** чтобы видеть статус

---

## ✅ Как проверить, что все работает?

В логах Render вы должны увидеть:
```
[OK] ✅ Конфиг создан: configs/_main.cfg
[INFO] ✅ Инициализация завершена
```

После этого бот должен запуститься и появится сообщение:
```
FunPay Cardinal v0.1.16.9
...
Привет! (`-`)/
```

---

## ❌ Если что-то не работает

### Бот не запускается?

1. **Проверьте логи:**
   - В Render откройте "Logs" 
   - Найдите строку с ошибкой

2. **Проверьте переменные окружения:**
   - `GOLDEN_KEY` - должно быть 32 символа
   - `TG_TOKEN` - должно содержать двоеточие и быть 45+ символов

3. **Если видите `[ERROR] ❌ GOLDEN_KEY или TG_TOKEN не установлены`:**
   - Откройте Settings → Environment
   - Убедитесь что переменные добавлены
   - Нажмите "Save" и перезагрузите сервис

4. **Если видите ошибку про формат GOLDEN_KEY:**
   - Проверьте что скопировали ВЕСЬ ключ (32 символа)
   - Уберите кавычки если случайно скопировали их

---

## 📋 Быстрая проверка данных

Перед развертыванием убедитесь:

- ✅ GOLDEN_KEY: Начало: `abc1...` Конец: `...d456` (всего 32 символа)
- ✅ TG_TOKEN: Формат: `123456789:ABCDef...` (есть двоеточие посередине)

---

## 🎯 На этом все!

После развертывания бот будет работать 24/7 на бесплатном плане Render.
